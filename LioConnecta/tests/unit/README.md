# Testes unitários

Camada coberta nesta fase:

- `mappers`
- `services`
- `validators`

Como rodar:

```bash
npm test
```

Ou no Windows:

```powershell
npm test
```

Cobertura atual:

- normalização de dados
- defaults/fallbacks
- composição da home
- integração services -> mappers
- validação de contrato dos JSONs
